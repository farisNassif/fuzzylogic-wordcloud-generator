package ie.gmit.sw.ai.V4;

public class RunnerV6GJfuzLogic {

	/* Something like that
	 * IF word IS frequent THEN relevance is high;
	 * IF word IS marginal THEN relevance is normal; 
	 * IF word IS infrequent THEN relevance is low;
	 */

}
